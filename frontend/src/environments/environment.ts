export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000',
  /** When true, skip login and treat as authenticated (for local dev; avoid refresh logins). See backend README for AUTH_DEV_ALWAYS_LOGGED_IN. */
  devAlwaysLoggedIn: false,
};
